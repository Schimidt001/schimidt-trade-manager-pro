[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && corepack enable && pnpm install --frozen-lockfile && pnpm -C apps/api build"

[deploy]
startCommand = "cd ../.. && pnpm -C apps/api start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
