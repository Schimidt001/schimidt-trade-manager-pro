[phases.setup]
nixPkgs = ["nodejs_20", "corepack"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@latest --activate",
  "pnpm install --frozen-lockfile"
]

[start]
cmd = "echo 'Use service-specific start commands'"
